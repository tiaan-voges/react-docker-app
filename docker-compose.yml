version: "3.9"

services:
  react:
    container_name: react-dev
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ${PWD}/frontend:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true

